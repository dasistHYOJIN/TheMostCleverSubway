package com.example.hyo_jin.themostcleversubway.View;

import android.content.Context;
import android.content.res.TypedArray;
import android.graphics.Bitmap;
import android.graphics.drawable.Drawable;
import android.support.annotation.Nullable;
import android.util.AttributeSet;
import android.view.View;
import android.view.ViewParent;

import com.example.hyo_jin.themostcleversubway.R;

/**
 * Created by Hyo-Jin on 2018-06-09.
 * This View makes vertical progress bar
 */

public class VerticalProgressBar extends View {
    public VerticalProgressBar(Context context) {
        super(context);
    }
/*
    private static final int MAX_LEVEL = 10000;

    int mMinWidth, mMaxWidth, mMinHeight, mMaxHeight;

    private int mProgres;
    private int mSecondaryProgress;
    private int mMax;

    private Drawable mProgressDrawable;
    private Drawable mCurrentDrawable;
    Bitmap mSampleTile;
    private boolean mNoInvalidate;
    private RefreshProgressRunnable mRefreshProgressRunnable;
    private long mUiThreadId;

    protected int mScrollX;
    protected int mScrollY;
    protected int mPaddingLeft;
    protected int mPaddingRight;
    protected int mPaddingTop;
    protected int mPaddingBottom;
    protected ViewParent mParent;

    public VerticalProgressBar(Context context) {
        super(context, null);
    }

    public VerticalProgressBar(Context context, @Nullable AttributeSet attrs, int defStyle) {
        super(context, attrs, defStyle);

        mUiThreadId = Thread.currentThread().getId();
        initProgressBar();

        TypedArray a = context.obtainStyledAttributes(attrs, R.styleable.ProgressBar, defStyle, 0);

        mNoInvalidate = true;

        Drawable drawable = a.getDrawable(R.styleable.Pro)
    }*/
}
