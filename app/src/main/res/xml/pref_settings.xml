<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <PreferenceCategory android:title="지역 선택">

        <ListPreference
            android:defaultValue="0"
            android:dependency="useUserName"
            android:entries="@array/location"
            android:entryValues="@array/location_values"
            android:key="location"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null"
            android:title="이름 공개 범위" />

    </PreferenceCategory>

    <PreferenceCategory android:title="푸시 알림 설정">

        <SwitchPreference
            android:defaultValue="false"
            android:key="autoUpdate"
            android:summary="자동 업데이트를 설정합니다"
            android:switchTextOff="OFF"
            android:switchTextOn="ON"
            android:title="자동 업데이트" />

       <CheckBoxPreference
            android:defaultValue="false"
            android:key="useUserName"
            android:summaryOff="사용자의 이름을 사용하지 않습니다"
            android:summaryOn="사용자의 이름을 사용합니다"
            android:title="사용자 이름 사용" />

        <EditTextPreference
            android:defaultValue="Mir"
            android:dependency="useUserName"
            android:key="userName"
            android:maxLines="1"
            android:selectAllOnFocus="true"
            android:singleLine="true"
            android:title="사용자 이름 설정" />

        <ListPreference
            android:defaultValue="0"
            android:dependency="useUserName"
            android:entries="@array/userNameOpen"
            android:entryValues="@array/userNameOpen_values"
            android:key="userNameOpen"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null"
            android:title="이름 공개 범위" />

    </PreferenceCategory>

    <PreferenceCategory android:title="개발 정보">

        <Preference
            android:summary="개발자 정보를 확인합니다"
            android:title="개발자 정보" >
            <intent
                android:targetClass="com.example.hyo_jin.themostcleversubway.Activity.DevInfoActivity"
                android:targetPackage="com.example.hyo_jin.themostcleversubway.Activity" />
        </Preference>

    </PreferenceCategory>

</PreferenceScreen>